# Relax XRD Plotter 使用说明

> 一款用于绘制 X 射线衍射（XRD）谱图的桌面软件，支持多谱图叠加/堆叠、自动标峰、手动标注、图例独立定位、工程文件保存与一键导出高质量图片。

---

## 一、快速开始

1. **运行软件**
   - 便携版：解压压缩包后双击 `Relax XRD Plotter.exe` 即可（无需安装 Python）。
   - 源码版：在本目录执行 `python main.py`。

2. **导入数据**（任选其一）
   - 点击左侧「📂 打开数据文件」按钮，选择数据文件；
   - 或将数据文件直接**拖拽**到左侧「拖拽数据到这里」区域；
   - 示例数据位于 `示例数据\` 文件夹，可直接拖入体验。

3. **调整外观**（按需）
   - 在「🎨 绘图」标签页设置图尺寸、坐标轴范围、标题、图例位置等；
   - 在右侧「✳ 标注」面板勾选「当前谱图启用标峰」自动标出衍射峰。

4. **导出结果**
   - 点击图下方「💾 导出图片」，选择 PNG / TIFF / SVG / PDF 保存；
   - 或点击「📄 保存处理文件」，把当前图连同所有编辑设置存为 `.xrdproj` 工程文件，下次直接打开继续编辑。

> 大多数设置修改后会自动刷新；如未生效，点图下方「🔄 刷新绘图」即可。

---

## 二、界面总览

软件采用**三栏布局**：

| 区域 | 内容 |
|------|------|
| 左侧「📁 数据」标签页 | 数据文件管理、绘图模式、颜色设置 |
| 左侧「🎨 绘图」标签页 | 图尺寸、坐标轴、标题标签、图例、边框 |
| 右侧「✳ 标注」面板 | 自动标峰、手动标记 |
| 图正下方按钮栏 | 刷新绘图、导出图片、保存处理文件 |

界面右上角可切换 **UI 主题**（9 套：现代浅色/现代深色/卡通乐园/薄荷清新/Deep Space 等）。

---

## 三、数据区（📁 数据）

### 1. 数据文件管理
- **📂 打开数据文件**：弹出文件选择框导入数据。
- **拖拽数据到这里**：把数据文件拖入此区域即可导入。
- **文件列表**：列出已导入的样品，选中后下方控件作用于该样品。
- **图例名称**：选中样品后输入名称，点「应用」或回车，修改其在图例中的显示名。
- **当前线宽**：0.5–8.0，实时调整选中样品曲线粗细。
- **当前线型**：实线 / 虚线 / 点划线 / 点线 / 稀疏虚线 / 稀疏点划线。
- **添加 / 删除 / ↑ / ↓**：导入更多文件、删除选中文件、调整样品在图中的叠放顺序（顺序影响堆叠偏移与图例排列）。

### 2. 绘图模式
- **单图模式**：所有曲线叠加在同一坐标系。
- **堆叠模式**：曲线依次向上偏移，便于对比多组样品；偏移量由「偏移系数」滑块/输入框控制（系数越大间距越大）。

### 3. 颜色设置
- **预设配色**：下拉选择配色方案（Nature、Science、Custom、莫兰迪、马卡龙、赛博朋克、海洋深蓝、森林绿意、暖橙金秋、糖果乐园，以及多套「原神」主题配色），一键应用到全部样品。
- **单样品取色**：每个样品对应一个色块，点击可单独设置该曲线的颜色。

---

## 四、绘图区（🎨 绘图）

### 1. 图尺寸
- 设置图片的**宽度**与**高度**（单位：英寸），决定导出图片的画幅比例。

### 2. 坐标轴
- **X 轴 / Y 轴 最小·最大值**：默认显示「自动」。点击输入框聚焦时会自动全选「自动」二字，**直接输入数字即可覆盖**；留空或重置为「自动」则软件自动适配。
- **主刻度间隔**：X / Y 方向主轴刻度步长，同样支持「自动」+ 直接输入数字。
- **刻度方向**：`in`（朝内）/ `out`（朝外）/ `inout`（双向）。
- **重置为自动**：一键恢复全部坐标轴范围为自动。

### 3. 标题标签
- **标题 / X 轴标签 / Y 轴标签**：对应图上的文字内容（X 轴默认 `2θ (°)`，Y 轴默认 `Intensity (a.u.)`）。
- **标题字号 / 标签字号 / 刻度字号**：分别控制各层级文字大小。
- **字体**：默认 **Times New Roman（新罗马）**，可在下拉中切换其它字体。
- **标题加粗**：默认**不勾选**（不加粗）；勾选后标题加粗显示。
- **显示 X/Y 轴标签**：开关是否在图上显示坐标轴标题。

### 4. 图例
- **显示图例**：总开关。
- **图例布局**（三种模式）：
  - **集中图例**：传统统一图例框，可在「位置」中选择 9 个方位（upper right、upper left 等）。
  - **谱图上方**：每个样品名称直接标注在对应曲线的**右上方**，颜色与曲线一致，无独立图例框。
  - **手动拖动**：标签初始在曲线右上方，**用鼠标可逐个拖到任意位置**；拖动后的位置会被记住，刷新不丢失。
- **图例字号 / 图例字体**：控制图例文字大小与字体（默认新罗马）。
- **显示图例边框**：是否给图例框加边框。

### 5. 边框
- **加粗坐标边框**：勾选后坐标轴线加粗。
- **边框线宽**：设置边框粗细。

---

## 五、标注区（✳ 标注，右侧面板）

### 1. 标峰设置（自动标峰）
- **当前谱图启用标峰**：勾选后，对**当前选中的样品**自动查找并标注衍射峰（需在数据文件列表里先选中该样品）。
- **标注内容**：
  - `符号+峰位`：在峰处画符号并显示峰位数值；
  - `仅峰位`：只显示峰位数值；
  - `仅符号`：只在峰处画符号。
- **峰数量**：
  - `手动`：标出数量固定的前 N 个最强峰（由「手动峰数」设定）；
  - `自动`：按「最小峰间距」自动判定峰的数量。
- **最小峰间距**：自动模式下，两个峰之间至少需要间隔的 2θ 角度，避免密集噪声被误判为峰。
- **峰位小数位**：峰位数值保留几位小数。
- **符号类型**：10 种可选——圆形、正方形、三角形、倒三角形、菱形、五角星、六边形、五边形、十字形、叉形。
- **符号颜色 / 文字颜色**：分别设置峰标记符号与峰位数值的颜色。

### 2. 手动标记
- **启用手动标记**：勾选后开启手动标注功能。
- **添加标记**：在图上**左键点击**即可放置一个标记，使用下方设置的符号、颜色、大小、默认文字。
- **符号类型**：同样支持上述 10 种符号。
- **选择颜色 / 大小**：设置新标记的样式（大小 20–300）。
- **新加符号默认文字**：每次点击添加时自动带上的文字。
- **修改当前符号文字**：在列表选中某标记，输入文字后点「应用文字」（或修改「当前符号文字」框）即可改其标注。
- **标记列表**：列出所有手动标记，选中后：
  - 「删除选中」：移除该标记；
  - 「撤销」：删除最近添加的一个；
  - 「清空」：清空全部手动标记；
  - 「↑ ↓ ← →」：以小幅步长微调选中标记的精确位置。
- **单击已有标记**可选中它；在「手动拖动」图例模式下，拖动的是图例标签而非手动标记，二者互不干扰。

---

## 六、图下方按钮栏

| 按钮 | 功能 |
|------|------|
| 🔄 刷新绘图 | 应用所有设置并重绘当前图。 |
| 💾 导出图片 | 导出为 **PNG / TIFF / SVG / PDF**（在弹窗中选择格式与路径）。 |
| 📄 保存处理文件 | 保存当前成果，见下方「工程文件」。 |

### 工程文件（.xrdproj）
「保存处理文件」对话框提供两类保存：

- **XRD 工程文件 (*.xrdproj)**：保存**数据 + 全部编辑信息**，包括：
  - 每个样品：图例名、颜色、线宽、线型、标峰开关与符号配置、图例标签位置；
  - 全局：绘图模式、偏移系数、图尺寸、坐标轴、标题标签、图例（含布局与位置）、边框、标峰参数、手动标记。
  - 下次直接**打开**或**拖入**该 `.xrdproj` 文件，即可在原有基础上继续编辑，无需重新设置。
- **CSV / TXT**：仅导出图上显示的数据（含堆叠偏移），每个样品两列（2θ + 强度），重名样品自动加 `_2` 后缀。

---

## 七、主题与外观

- 界面右上角「UI」下拉可切换 9 套界面主题，所有控件样式（含弹窗、下拉框）会整体跟随。
- 绘图区的字体、配色等由对应设置项控制，与 UI 主题相互独立。

---

## 八、支持的数据格式

**导入**：
- 文本类：`.xy`、`.dat`、`.csv`、`.txt`（自动识别逗号/制表符/分号/空格分隔，自动跳过表头）
- **Bruker RAW4.00 / D8 导出的 `.txt`**：仪器直接导出的格式，文件头部含 `;RAW4.00` 标识与 `[RawHeader]`/`[Data]` 等区块（数据形如 `5.0001, 507,`，带尾随逗号和空格填充）。软件会自动识别并读取 `[Data]` 区块的 2θ 与强度列，无需手动整理。
- Excel：`.xlsx`（读取第一个工作表）
- 工程文件：`.xrdproj`（恢复数据与全部编辑状态）

**示例数据**（位于 `示例数据\` 文件夹，可直接拖入体验）：
- `sample_data.csv` / `sample_data2.csv`：单条曲线的常规示例；
- `xrd_multi_column_demo.txt`：一个文件内含多条谱线（制表符分隔，`2theta / Sample_A / Sample_B / Sample_C`），适合演示**叠加 / 堆叠**多谱图；
- `xrd_multi_column_demo.xlsx`：同上结构的 Excel 多谱图示例；
- `passivated_ball_Pb.txt`（Bruker RAW 导出）：演示 Bruker 仪器原始数据直接读取。

**数据要求**：至少 2 列（第 1 列为 2θ 角度，后续列为强度），至少 2 行有效数值；多列强度会自动拆分为多条曲线。

**导出**：`.png`、`.tiff`、`.svg`、`.pdf`，以及工程文件 `.xrdproj`、数据 `.csv` / `.txt`。

---

## 九、常见问题与注意事项

- **杀毒误报**：便携版 exe 由 PyInstaller 打包，个别杀毒软件可能误报。属正常现象，将软件所在文件夹加入杀毒软件信任/白名单即可。
- **中文显示**：标题/标签默认字体为 Times New Roman（新罗马），中文样品名由系统字体兜底，可正常显示；如需统一中文样式，可在「标题标签」中改字体。
- **坐标不生效**：确认已输入有效数字（聚焦输入框后直接打字即可覆盖「自动」）；修改后若图未更新，点「刷新绘图」。
- **工程文件打不开**：确认文件扩展名为 `.xrdproj`，且由本软件同版本保存；拖入或「打开数据文件」均可载入。

---

## 十、作者信息

- 作者：qxh
- 单位：WIT
- 邮箱：qbaiyi@qq.com
