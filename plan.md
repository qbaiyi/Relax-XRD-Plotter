# XRD绘图软件开发计划

## 一、项目概述
开发一个带有图形界面的XRD（X射线衍射）绘图软件，支持单条/多条XRD光谱的绘制、堆叠显示、自由配色和轴设置。

## 二、技术选型
| 模块 | 技术 | 理由 |
|------|------|------|
| UI框架 | PyQt5 | 功能强大、界面美观、跨平台 |
| 绘图引擎 | matplotlib | Python科学绘图标准库，XRD绘图首选 |
| 数据处理 | numpy + pandas | 高效数值计算和数据解析 |
| 打包工具 | PyInstaller | 将Python程序封装为独立exe |

## 三、功能设计

### 3.1 数据加载
- 支持常见XRD数据格式：.xy, .dat, .csv, .txt, .xlsx
- 自动识别两列数据（2θ / Intensity）
- 支持同时导入多个文件
- 文件列表管理（添加、删除、排序）

### 3.2 绘图功能
- **单图模式**：单条XRD曲线绘制
- **堆叠模式**：多条XRD曲线在同一平面纵向偏移堆叠（最常用的XRD多图展示方式）
- 偏移量可调（自动/手动）
- 线型、线宽可调

### 3.3 颜色管理
- 每条曲线可独立选择颜色
- 提供预设配色方案（Nature、Science等期刊风格）
- 支持自定义颜色选择器

### 3.4 坐标轴设置
- X轴范围（2θ min / max）
- Y轴范围（Intensity min / max）
- 主刻度间距设置
- 次刻度开关
- 刻度方向（in/out/inout）
- 轴标签字体大小

### 3.5 标题与标签
- 图标题（内容、字体、大小、粗细）
- X轴标签（默认"2θ (°)"）
- Y轴标签（默认"Intensity (a.u.)"）
- 图例开关及位置

### 3.6 导出功能
- 导出为高分辨率图片（PNG、TIFF、SVG、PDF）
- DPI可调（默认300）
- 尺寸可调

## 四、项目结构
```
d:\xrd sof\
├── main.py              # 程序入口
├── xrd_plotter.py       # 核心绘图引擎
├── data_loader.py       # 数据加载与解析
├── ui_main.py           # 主窗口UI
├── ui_settings.py       # 设置面板UI
├── requirements.txt     # 依赖列表
├── build.bat            # 打包脚本
└── README.md            # 使用说明
```

## 五、UI布局设计
```
+--------------------------------------------------+
|  菜单栏: 文件 | 设置 | 帮助                        |
+--------------------------------------------------+
|  左侧面板 (300px)    |   右侧绘图区域              |
|  +----------------+  |   +--------------------+   |
|  | 数据文件列表    |  |   |                    |   |
|  | [添加] [删除]   |  |   |   matplotlib       |   |
|  +----------------+  |   |   绘图预览区        |   |
|  | 绘图模式选择    |  |   |                    |   |
|  | ○ 单图 ○堆叠   |  |   |                    |   |
|  +----------------+  |   |                    |   |
|  | 偏移量设置      |  |   |                    |   |
|  +----------------+  |   |                    |   |
|  | 颜色设置       |  |   |                    |   |
|  +----------------+  |   +--------------------+   |
|  | 轴设置         |  |                            |
|  +----------------+  |   [刷新] [导出]            |
|  | 标题/标签设置   |  |                            |
|  +----------------+  |                            |
+--------------------------------------------------+
```

## 六、开发步骤
1. 创建项目结构和依赖配置
2. 实现数据加载和解析模块
3. 实现核心绘图引擎（单图/多图堆叠）
4. 实现PyQt5主窗口UI
5. 实现轴设置、颜色选择、标题编辑面板
6. 实现导出功能
7. 配置PyInstaller打包脚本
8. 测试验证

## 七、关键实现细节

### 堆叠绘图算法
- 每条曲线沿Y轴方向偏移一个固定量
- 偏移量 = max_intensity × offset_factor
- offset_factor 默认 0.8，可手动调节
- 堆叠顺序与文件列表顺序一致

### 数据解析策略
- 尝试逗号、制表符、空格分隔
- 跳过头部非数字行
- 自动识别2θ列和强度列
